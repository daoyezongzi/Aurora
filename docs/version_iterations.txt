Aurora 版本迭代与目标

v0.1 最小可行
- 完成单股票 002372.SZ 的数据下载、训练、评估、推理闭环。
- 产出 outputs/model.pt、outputs/metrics.json、outputs/predictions.csv。
- 保证可本地运行和可一次性提交。

v0.2 课程达标
- 补充多模型对比与调参。
- 基线模型先做 LogisticRegression + MLP，并与 LSTM 对比。
- 完善实验图表和结论。

v0.3 稳健性增强
- 加入滚动验证。
- 做阈值调优与误差分析。
- 提升报告说服力与可解释性。

v0.4 OpenMLDB 落地
- 将时序特征工程迁移到 OpenMLDB SQL 流程。
- 做迁移前后的一致性与效率对比。

v1.0 交付冻结
- 整理最终代码包。
- 完成课程报告与 2 分钟答辩 PPT。

v1.1 课程后扩展
- 再对接 TradeEye，不影响课程交付节奏。

----------------------------------------
v0.2 当前实测结果（2026-05-23）
----------------------------------------

数据来源：
- outputs/v2/compare_metrics.csv
- outputs/v2/lstm/metrics_v2.json
- outputs/v2/logreg/metrics.json
- outputs/v2/mlp/metrics.json

实测总表：
1) lstm
- accuracy = 0.5170
- f1 = 0.0000
- precision = 0.0000
- recall = 0.0000
- roc_auc = 0.4546
- sample_count = 176

2) logreg
- accuracy = 0.5114
- f1 = 0.1569
- precision = 0.4706
- recall = 0.0941
- roc_auc = 0.5279
- sample_count = 176

3) mlp
- accuracy = 0.5568
- f1 = 0.6061
- precision = 0.5310
- recall = 0.7059
- roc_auc = 0.5625
- sample_count = 176

当前阶段结论：
- v0.2 目标“多模型对比”已达成，且三模型有明显差异。
- 当前最优模型是 mlp（f1/recall/roc_auc 都最高）。
- lstm 在当前特征和参数下退化明显（f1=0，几乎无可用分类能力）。
- logreg 可作为“保守基线”，但召回偏低。

建议的 v0.3 入口：
1) 在 mlp 基础上做阈值调优（先以 F1 为目标）。
2) 增加滚动验证，避免单次切分偶然性。
3) 增加误差分析（按日期段、涨跌分布、概率区间分层查看）。

----------------------------------------
v0.3 当前实测结果（2026-05-23）
----------------------------------------

数据来源：
- outputs/v3/rolling_metrics.csv
- outputs/v3/final/metrics.json
- outputs/v3/error_summary.json

一、滚动验证（3 folds）
- fold1: test_f1=0.6429, test_roc_auc=0.5358, best_threshold=0.39
- fold2: test_f1=0.6782, test_roc_auc=0.5994, best_threshold=0.30
- fold3: test_f1=0.6444, test_roc_auc=0.5621, best_threshold=0.37

解读：
- F1 在 0.64~0.68 之间，说明不同时间窗下结果有波动但不至于崩坏。
- best_threshold 明显低于 0.5，说明当前模型要提高召回需要更低阈值。

二、最终评估（固定测试集）
- threshold = 0.48
- accuracy = 0.5469
- f1 = 0.6133
- precision = 0.5349
- recall = 0.7188
- roc_auc = 0.5603
- confusion_matrix = [[36, 60], [27, 69]]

解读：
- 与 v0.2 的 mlp（f1=0.6061）相比，f1 有小幅提升。
- 召回高（0.7188），但误报（FP=60）仍偏多，属于“偏激进”策略。

三、误差分析摘要
- sample_count = 192
- ok_count = 105
- false_positive_count = 60
- false_negative_count = 27
- error_count = 87
- near_threshold_error_rate = 0.3103

解读：
- 错误中约 31.0% 发生在阈值附近（|up_prob-threshold|<=0.05）。
- 说明“阈值微调 + 置信度分层”仍有优化空间。

v0.3 阶段结论：
- “滚动验证 + 阈值调优 + 误差分析”三项目标已完成并可复现。
- 当前可将 mlp_v3 作为课程报告主线模型，后续在 v0.4 再做特征工程增强。

----------------------------------------
v0.3.1 补充改动与实测（2026-05-24）
----------------------------------------

改动内容：
1) 阈值调优目标从固定 F1 改为可配置目标（默认 f0.5）。
2) 新增概率校准能力（none/sigmoid/isotonic），默认 none。
3) 在 rolling/final 产物中新增阈值目标与校准参数字段。

最新数据来源：
- outputs/v3/rolling_metrics.csv
- outputs/v3/final/metrics.json
- outputs/v3/error_summary.json

最新结果（默认：thr_goal=f0.5, calib=none）：
1) 最终评估（fixed test）
- threshold = 0.50
- accuracy = 0.5417
- f1 = 0.5600
- precision = 0.5385
- recall = 0.5833
- roc_auc = 0.5559
- confusion_matrix = [[48, 48], [40, 56]]

2) 滚动验证（3 folds）
- fold1: test_f1=0.6429, best_threshold=0.39
- fold2: test_f1=0.6271, best_threshold=0.48
- fold3: test_f1=0.6234, best_threshold=0.49

3) 误差摘要
- false_positive_count = 48
- false_negative_count = 40
- near_threshold_error_rate = 0.3977

与 v0.3（上一轮默认 F1 目标）对比：
1) FP: 60 -> 48（误报下降）
2) FN: 27 -> 40（漏报上升）
3) f1: 0.6133 -> 0.5600（综合分数下降）

结论：
- v0.3.1 已具备“按业务偏好调阈值目标 + 可选概率校准”的能力。
- 当前默认参数更保守于误报，但会牺牲召回与总体 F1。
- 下一步建议在保持该框架下继续做特征增强，再重新搜索目标与阈值。

----------------------------------------
v0.3.2 特征筛选与再训练（2026-05-24）
----------------------------------------

改动内容：
1) 在数据管线中补充了扩展特征池（收益率/波动率/价量结构特征）。
2) 做了增量筛选后，默认特征采用 base8 + ret_5（避免全量新特征导致模型失稳）。
3) v3 脚本增加“processed 特征列缺失时自动回退 prepare_data”逻辑，防止旧处理文件导致运行失败。

最新默认配置（default.yaml）：
- open, high, low, close, vol, pct_chg, ma5, ma10, ret_5

最新结果（默认命令 run_aurora.bat v3）：
1) final（fixed test）
- threshold = 0.45
- accuracy = 0.5632
- f1 = 0.6344
- precision = 0.5414
- recall = 0.7660
- roc_auc = 0.5479
- confusion_matrix = [[35, 61], [22, 72]]

2) error_summary
- false_positive_count = 61
- false_negative_count = 22
- error_count = 83
- near_threshold_error_rate = 0.3373

与 v0.3.1 默认（f0.5 + none）对比：
1) f1: 0.5600 -> 0.6344（提升）
2) accuracy: 0.5417 -> 0.5632（提升）
3) precision: 0.5385 -> 0.5414（小幅提升）
4) recall: 0.5833 -> 0.7660（提升）
5) roc_auc: 0.5559 -> 0.5479（小幅下降）

结论：
- v0.3.2 当前更偏“提高命中率/F1”，代价是误报仍偏高（FP=61）。
- 若后续目标改为“显著控误报”，需要在阈值目标里再加入 precision 下限约束。

----------------------------------------
v0.3.3 稳态修复（2026-05-24）
----------------------------------------

改动内容：
1) 阈值选择从“只取最高分”改为“近最优带内取更稳阈值”。
   - 新增参数：thr_eps（近最优容差）、thr_anchor（稳定锚点）。
2) 配置选择从“只看均值”改为“均值-波动惩罚”。
   - 新增参数：cfg_std_pen（分数波动惩罚）、cfg_thr_pen（阈值波动惩罚）。
3) 拆分了阈值与配置的容差控制。
   - 新增参数：cfg_eps（配置近最优容差）。
4) 新增配置级评估产物，便于排查“哪组超参跨时间窗最稳”。
   - outputs/v3/config_metrics.csv
   - outputs/v3/config_metrics.json

最新数据来源：
- outputs/v3/rolling_metrics.csv
- outputs/v3/config_metrics.csv
- outputs/v3/final/metrics.json
- outputs/v3/error_summary.json

最新结果（默认命令 run_aurora.bat v3）：
1) final（fixed test）
- threshold = 0.46
- accuracy = 0.5474
- f1 = 0.6126
- precision = 0.5313
- recall = 0.7234
- roc_auc = 0.5479
- confusion_matrix = [[36, 60], [26, 68]]

2) rolling（3 folds）
- fold1: best_threshold=0.33, test_f1=0.6786
- fold2: best_threshold=0.50, test_f1=0.5392
- fold3: best_threshold=0.46, test_f1=0.6079

3) error_summary
- false_positive_count = 60
- false_negative_count = 26
- error_count = 86
- near_threshold_error_rate = 0.3488

与 v0.3.2 对比：
1) 阈值稳定性：0.33/0.68/0.45 -> 0.33/0.50/0.46（跨折抖动收敛）
2) fold2 崩塌修复：test_f1 0.3066 -> 0.5392（显著改善）
3) final 指标：f1 0.6344 -> 0.6126（小幅回落，换取稳态）

结论：
- v0.3.3 的核心收益是“稳态优先”：先修复跨折阈值跳变和单折崩塌，再做下一轮提分。
- 当前已达到“可直接运行并拿到模型数据与图表”的稳定交付状态。

----------------------------------------
v0.3.4 门槛约束 + final阈值稳态（2026-05-24）
----------------------------------------

改动内容：
1) 阈值评分增加 precision/recall 门槛惩罚。
   - 新增参数：min_precision、min_recall、goal_penalty。
2) final 阈值来源可配置。
   - 新增参数：final_thr_mode（rolling_median / val）。
   - 默认 rolling_median：使用 rolling best_threshold 的中位数。
3) final 阈值统一做“落格”处理。
   - 先裁剪到 [thr_min, thr_max]，再吸附到 thr_step 网格。
4) 稳定性惩罚默认加强。
   - cfg_std_pen: 0.20
   - cfg_thr_pen: 0.12
5) final 指标新增可追踪字段：
   - final_threshold_mode / final_threshold_source
   - rolling_threshold_median / val_best_threshold
   - min_precision / min_recall / goal_penalty

最新数据来源：
- outputs/v3/rolling_metrics.csv
- outputs/v3/config_metrics.csv
- outputs/v3/final/metrics.json
- outputs/v3/error_summary.json

最新结果（默认命令 run_aurora.bat v3）：
1) final（fixed test）
- threshold = 0.45
- accuracy = 0.5632
- f1 = 0.6344
- precision = 0.5414
- recall = 0.7660
- roc_auc = 0.5479
- confusion_matrix = [[35, 61], [22, 72]]
- final_threshold_source = rolling_median
- rolling_threshold_median = 0.45
- val_best_threshold = 0.46

2) rolling（3 folds）
- fold1: best_threshold=0.33, test_f1=0.6786
- fold2: best_threshold=0.67, test_f1=0.3165
- fold3: best_threshold=0.45, test_f1=0.6174

3) error_summary
- false_positive_count = 61
- false_negative_count = 22
- error_count = 83
- near_threshold_error_rate = 0.3373

与 v0.3.3 对比：
1) final 指标：f1 0.6126 -> 0.6344（提升）
2) final 阈值来源：由 val 单点偏置转为 rolling 中位阈值可追踪
3) rolling 稳定性：fold2 仍偏弱（需继续修）

结论：
- v0.3.4 已把“门槛约束 + final 阈值稳态”机制落地并可复现。
- final 指标提升，但 rolling 的 fold2 退化问题尚未根治，下一步应继续定向修复 fold2。

----------------------------------------
v0.3.5 训练轮次收敛修复（2026-05-24）
----------------------------------------

改动内容：
1) 新增参数 `--mlp-max-iter`，并将默认值提升到 900。
2) 完成 max_iter 收敛扫描：
   - 500 / 650 / 800 / 850 / 900 / 950 / 1000 / 1200
3) 收敛结论：
   - 900 左右已接近收敛；
   - 950 以上进入平台区，继续拉高轮次收益很小。

最新数据来源：
- outputs/v3/iter_sweep_summary.csv
- outputs/v3/rolling_metrics.csv
- outputs/v3/final/metrics.json
- outputs/v3/error_summary.json

最新结果（默认命令 run_aurora.bat v3，mlp_max_iter=900）：
1) final（fixed test）
- threshold = 0.42
- accuracy = 0.5316
- f1 = 0.6426
- precision = 0.5161
- recall = 0.8511
- roc_auc = 0.5479
- confusion_matrix = [[21, 75], [14, 80]]

2) rolling（3 folds）
- fold1: best_threshold=0.33, test_f1=0.6786
- fold2: best_threshold=0.42, test_f1=0.6667
- fold3: best_threshold=0.45, test_f1=0.6261
- avg_test_f1 = 0.6571
- min_test_f1 = 0.6261

3) error_summary
- false_positive_count = 75
- false_negative_count = 14
- error_count = 89
- near_threshold_error_rate = 0.2809

与 v0.3.4（默认 500）对比：
1) rolling 稳定性显著提升（min_test_f1 由 0.3165 提升到 0.6261）
2) final f1 提升（0.6344 -> 0.6426）
3) precision 下降、FP 上升（误报风险增加）

结论：
- `mlp_max_iter=900` 是当前“稳定性更好”的收敛点。
- 下一步要在保持该稳定性的基础上，继续压误报（优先优化 precision 约束与阈值策略）。

----------------------------------------
v0.3.6 v4门槛联合执行（2026-05-24）
----------------------------------------

本轮联合执行内容（一次性联动）：
1) 固定收敛轮次：mlp_max_iter=900
2) 门槛约束：min_precision=0.56, min_recall=0.62, goal_penalty=1.2
3) final阈值来源：final_thr_mode=val

联合实验产物：
- outputs/v3/v4_gate_combo_summary.csv

默认配置实测（run_aurora.bat v3）：
1) final（fixed test）
- threshold = 0.46
- accuracy = 0.5474
- f1 = 0.6126
- precision = 0.5313
- recall = 0.7234
- roc_auc = 0.5479
- confusion_matrix = [[36, 60], [26, 68]]

2) rolling（3 folds）
- fold1: test_f1=0.6643
- fold2: test_f1=0.6667
- fold3: test_f1=0.6261
- avg_test_f1 = 0.6523
- min_test_f1 = 0.6261
- std_test_f1 = 0.0186

结论：
- 与“高召回高误报”方案相比，本方案在保持 rolling 稳定性的同时，将 FP 控制在 60 附近。
- 作为“下一步直接进入 v4”的默认入口更合适：稳态可复现、误报可控、流程已固化。

----------------------------------------
v0.4 OpenMLDB风格时序特征迁移与对比（2026-05-24）
----------------------------------------

改动内容：
1) 在数据管线中新增 `feature_engine`：
   - pandas（迁移前）
   - sql（迁移后，OpenMLDB风格窗口SQL实现）
2) 新增 v4 对比脚本：
   - scripts/train_v4_migration_compare.py
3) bat 增加 v4 模式：
   - run_aurora.bat v4
   - run_aurora.bat v4-refresh

最新数据来源：
- outputs/v4/feature_consistency.csv
- outputs/v4/feature_consistency_summary.json
- outputs/v4/compare_metrics.csv
- outputs/v4/compare_summary.json
- outputs/v4/pandas/final/metrics.json
- outputs/v4/sql/final/metrics.json

一、特征一致性（迁移前 vs 迁移后）
- pandas_rows = 1293
- sql_rows = 1293
- joined_rows = 1293
- label_mismatch_count = 0
- feature_mean_abs_diff_avg = 1.30e-16
- feature_max_abs_diff_max = 7.11e-15

结论：
- SQL 迁移后的特征与 pandas 基本完全一致（仅浮点误差级差异）。

二、训练结果对比（同参数）
- final_f1: 0.5882 vs 0.5882（差值 0.0000）
- final_precision: 0.5200 vs 0.5200（差值 0.0000）
- final_recall: 0.6771 vs 0.6771（差值 0.0000）
- final_accuracy: 0.5260 vs 0.5260（差值 0.0000）
- final_roc_auc: 0.5474 vs 0.5474（差值 0.0000）
- rolling_avg_f1: 0.6050 vs 0.6050（差值 0.0000）
- rolling_min_f1: 0.5083 vs 0.5083（差值 0.0000）
- FP/FN: 60/31 vs 60/31

结论：
- 迁移前后训练表现一致，说明时序特征工程迁移成功。
- v0.4 的“迁移 + 前后对比”目标已达成。

----------------------------------------
课程提交冻结建议（基于大作业说明，2026-05-24）
----------------------------------------

推荐冻结版本：
1) 交付主线冻结在 v0.4（已满足“流程完整 + 对比充分 + 可运行”）。
2) 若时间紧，至少保证 v0.3 + v0.4 两套输出完整。

最简达标目标：
1) 方向选择：方向1（自主选题，结构化时序二分类）。
2) 完整流程：数据准备 -> 训练评估 -> 优化 -> 对比 -> 结论。
3) 关键对比：
- 模型层：v0.2（LSTM/LogReg/MLP）
- 特征工程层：v0.4（pandas/sql）

可选扩展（不影响本次提交达标）：
1) 真正部署 OpenMLDB 服务并改为在线特征计算。
2) 新增更多股票标的做跨标的泛化验证。
